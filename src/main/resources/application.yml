spring:
  profiles:
    active: db

# Swagger(OpenAPI) springdoc 설정
springdoc:
  api-docs:
    path: /api-docs   # OpenAPI 명세 경로 (기본: /v3/api-docs)
  swagger-ui:
    path: /swagger-ui # Swagger UI 경로 (기본: /swagger-ui.html)

